# LLM Router v5.0 Dependencies
# Install: pip install -r requirements-v5.txt
# Compatible with Python 3.14+

# Core NLP (updated for Python 3.14 compatibility)
# spacy>=3.8.0  # REMOVED: Python 3.14 incompatibility, replaced with regex-based approach
transformers>=4.40.0
torch>=2.3.0

# Sentence Embeddings (multilingual intent classification)
sentence-transformers>=3.0.0

# Machine Learning
scikit-learn>=1.5.0
numpy>=1.26.0

# Token Management
tiktoken>=0.7.0

# Utilities
tqdm>=4.66.0

# Web Framework (Enterprise API)
flask>=3.0.0
flask-cors>=4.0.0

# Note: spaCy removed due to Python 3.14 incompatibility
# NLP processing now uses transformers (BERT) + regex patterns
