# Vercel deployment dependencies (slim — no ML/torch)
# For full v5 NLP/ML deps, see requirements-v5.txt
flask>=3.0.0
flask-cors>=4.0.0
certifi

# TokenRouter auth dependencies
bcrypt>=4.0.0
PyJWT>=2.8.0
aiosqlite>=0.19.0
