# ⚡ LLM Router v5.0 빠른 시작

> 1분 안에 시작하기 - 최소한의 설명으로 바로 사용

---

## 🎯 30초 요약

```bash
# 1. 설치
pip3 install -r requirements-v5.txt

# 2. 실행
python3 llm_router_v5.py "요청" --v5 --compress

# 끝!
```

---

## 📦 설치

### 전체 기능 (v5.0)
```bash
pip3 install -r requirements-v5.txt
```

### 기본 기능만 (v4.0)
```bash
pip3 install tiktoken
```

---

## 🚀 3가지 실행 방법

### 1️⃣ CLI (명령줄)

```bash
python3 llm_router_v5.py "로그인 기능 만들어줘" --v5 --compress
```

### 2️⃣ GUI (그래픽)

```bash
python3 router_gui.py
```

### 3️⃣ Web (브라우저)

```bash
python3 web_server.py
# http://localhost:8080
```

---

## 🎛️ 자주 쓰는 옵션

```bash
python3 llm_router_v5.py "요청" \
  --v5                      # v5 엔진 (AI 기능)
  --compress                # 토큰 40-50% 절감
  --compression-level 2     # 압축 강도 (1=약, 2=중, 3=강)
  --show-stats              # 통계 표시
  --friendly                # 친절한 설명
  --economy balanced        # 비용 모드
```

---

## 💡 실전 예제

### 코드 분석
```bash
python3 llm_router_v5.py "app.py 버그 찾아줘" --v5
```

### 기능 구현
```bash
python3 llm_router_v5.py "회원가입 API 만들어줘" --v5 --compress
```

### 복잡한 작업
```bash
python3 llm_router_v5.py \
  "1. DB 설계 2. API 구현 3. 테스트 작성" \
  --v5 --compress --show-stats
```

---

## 🆚 v4.0 vs v5.0

| 사용 시나리오 | 권장 버전 | 명령어 |
|--------------|----------|--------|
| 빠른 작업 | v4.0 | `python3 llm_router.py "요청"` |
| 토큰 절약 | v5.0 | `--v5 --compress` |
| AI 분석 | v5.0 | `--v5 --intent-detect` |
| 우선순위 | v5.0 | `--v5 --smart-priority` |

---

## 🔧 Economy 모드

| 모드 | 언제 쓰나? | 옵션 |
|------|-----------|------|
| `strict` | 연습/테스트 (최저 비용) | `--economy strict` |
| `balanced` | 일반 작업 (기본값) | `--economy balanced` |
| `quality` | 중요 작업 (품질 우선) | `--economy quality` |

---

## 🗜️ 압축 레벨

| Level | 절감률 | 품질 | 언제? |
|-------|--------|------|------|
| 1 | ~30% | 최고 | 정밀 작업 |
| 2 | ~40% | 좋음 | 일반 작업 (권장) |
| 3 | ~50% | 보통 | 간단 작업 |

---

## ❓ 1분 문제 해결

### "BERT 실패" 에러
```bash
pip3 install --upgrade transformers
```

### "압축 안 됨"
```bash
# 레벨 올리기
--compression-level 3
```

### "포트 충돌" (Web UI)
```bash
python3 web_server.py --port 9000
```

---

## 📚 더 알아보기

- **상세 가이드**: [USER_GUIDE.md](USER_GUIDE.md)
- **전체 문서**: [README.md](README.md)
- **설계 문서**: [docs/02-design/](docs/02-design/)

---

## 🎓 학습 순서 (추천)

1. **Day 1**: CLI 기본 사용 (`--v5 --compress`)
2. **Day 2**: GUI로 옵션 실험
3. **Day 3**: Web UI로 팀 협업
4. **Day 4**: 통계 분석 (`--show-stats`)
5. **Day 5**: 고급 옵션 (compression-level, economy)

---

## 💬 자주 묻는 질문 (Top 3)

### Q1. v5가 v4보다 느린가요?
A. 0.5초 더 걸리지만 토큰 40-50% 절감으로 비용은 더 저렴합니다.

### Q2. 한국어만 되나요?
A. 한국어/영어 모두 지원. Groq API로 자동 번역도 가능.

### Q3. 무료인가요?
A. 코드는 무료(MIT). Groq API는 별도 (무료 tier 있음).

---

**시작하기**: `python3 llm_router_v5.py "hello world" --v5` 👈 지금 실행해보세요!
